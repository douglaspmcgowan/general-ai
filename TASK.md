# Task

## Goal

Finish repository-first cross-device reconstruction from the canonical harness and project GitHub authorities, preserve approved Obsidian configuration and declared external project data, retire or map legacy roots, and prove the complete flow on another computer.

## Active

- None.

## Queue

- [ ] T4 — Prove the merged onboarding flow on the receiving computer | after: B1, B2, B4 | implementation owner: [harness issue 18](https://github.com/pyrgos-ai/doug-harness/issues/18) | evidence: a receipt tied to the merged harness commit proves install/stamp, approved Obsidian bundle, PATH/executables, GitHub discovery, declared project verifiers/adapters, Bitwarden token import/read-back, and current General AI, Agent Brain, and Docket access | owner: receiving computer | class: SEQUENTIAL.

## Blocked

- [!] B1 — Publish the current approved Obsidian bundle in canonical harness onboarding | owner: active harness owner | evidence required: `webviewer=true`, pinned `html-page-viewer@0.1.1` package/checksums, receiving-side backup and restore fixture, refreshed manifests, reviewed merge, and merged-master install.
- [!] B2 — Publish the exact mutable-runtime verifier repair | owner: active harness owner | evidence required: harness `master` accepts only `logs/stale-agent-process-cleanup.jsonl` and `state/permissions-snapshot.json`, rejects undeclared siblings, and passes focused plus installed verification.
- [!] B3 — Restore the consolidated task-state hook suite to green | owner: active harness owner | current evidence: `Test-AgentProjectState.test.ps1` passes; `task-state-reminder.test.js` passes 51 and fails 6 chain/PROMPT_LOG cases.
- [!] B4 — Move prerequisite discovery ahead of Obsidian and Project Data mutation | owner: active harness owner | evidence required: blank-profile onboarding returns a complete `ATTENTION` inventory before either dependent stage can fail, and a blank-profile regression passes.

## Needs decision

None.

Future cloud implementation, Agent Brain automation, and preserved OneDrive data retirement remain parked in `BACKBURNER.md`.

## Constraints

- Preserve `C:\Users\dougl\My Drive\Project Data`; do not delete, rename, or repurpose it.
- Keep the 14-repository cloud rollout plan-only through `pyrgos-ai/doug-harness` issue #16.
- Do not recreate Drive Capsule, Password Manager scaffolds, or the retired 3/4/3 retention policy.
- Do not touch `_backups`, `research\docket-briefs`, the preserved OneDrive tree, credential values, protected vault paths, or another task's worktree.
- This task owns only the isolated General AI access/state branch. The canonical harness-owner worktree remains read-only.

## Completed

- [x] T1 — Publish current phone and agent pickup context | evidence: General AI PR 5 merged at `cec979f`; the access index uses the stable Docket card `agent-harness--cross-device-access-index-2026-07-31`; Agent Brain PR 3 merged the constrained refresh proposal at `fd584d1`; later transport updates preserve the same three-surface publication contract.
- [x] T2 — Reconcile durable General AI state with current authorities | evidence: the closeout reconciles TASK, STATUS, LOG, machine context, and provenance against General AI `cec979f`, Agent Brain `fd584d1`, the 174-card Docket store, and the current vault copy; project verification and independent review pass.
- [x] T5 — Publish one value-safe receiving-device receipt schema and refresh the cross-device access index | evidence: PR 10 merged at `84f1cf6`; the schema compiles as JSON Schema 2020-12, accepts one positive fixture, and rejects twelve negative fixtures covering credential fields in the Bitwarden and harness sections, retention above five snapshots, a SQLite adapter missing its retention block, a retention block on a non-SQLite adapter, `preserveNewestVerified` false, a duplicated access surface, a missing fourth surface, an abbreviated harness commit, an unknown top-level section, a wrong onboarding entry, and a wrong Bitwarden import variable; Gitleaks 8.30.1 found no leaks; the access-index body matches across Git, the active vault, and Docket at SHA-256 `a81eae1b7f98e5d00b16eba447693f67f0c35bc35e34ec7407c7ed680d93621e`; the brokered publication pushed 173 cards and refused 0 | owner: isolated General AI branch | class: MAIN.
- [x] T6 — Close the phone-safe entrypoint gaps found by the read-only link audit | evidence: all 15 access-index links resolve, with every private 404 confirmed as an auth gate over a path that exists on its default branch; the index now links `START-HERE.md`, both secret manifests, `recovery/`, `STATUS.md`, `TASK.md`, `MAP.md`, `MIGRATION.md`, both project manifests, and the BitLocker readiness brief, and adds a lost-private-access fallback; the two entrypoints now agree that the v2 machine-readable context is current; the vault START HERE brief was rewritten with absolute URLs so it works off-repository | owner: isolated General AI branch | class: MAIN.
- [x] T3 — Resolve every attention-required project Git transport | evidence: Boundaries Reader plus Bible Name Search, Contact Form Caller, Drive Organizer, Grandpa Help, and Motion to Dismiss have reviewed portable baselines on their remote default branches with `agent-project`, green latest hosted Gitleaks scans, complete declared Git-backed skill bundles, and preserved fail-closed local hooks; `general-claude`, `harness`, and `idetc-writing-ide` retain explicit consolidation mappings; remaining project-data and licensing attention is declared separately in the fleet inventory | owner: project fleet | class: PARALLEL.
- [x] C1 — Replace Drive Capsule distribution with the canonical repository root `ONBOARDING` flow.
- [x] C2 — Adopt SQLite retention of 2 daily, 2 weekly, and 1 monthly bucket, always preserving the newest verified snapshot and no more than five distinct snapshots.
- [x] C3 — Preserve completed cloud, retrospective, second-brain, and graph-planning research in GitHub, the active Obsidian vault, and Docket.
- [x] C4 — Publish the phone-safe access index and pickup context through General AI PR 3 at `9fa51b6`.
- [x] C5 — Publish the portable Agent Brain contract through private mirror PR 2 at `337d615`.
- [x] C6 — Publish the 27-root portability inventory through General AI PR 4 at `340a05f`.
- [x] Q1 — Answer whether an Antigravity agent or a Cursor agent can operate a browser, natively or through Playwright | evidence: Google Antigravity ships a browser subagent that clicks, scrolls, types, reads console logs, and captures DOM, screenshots, markdown, and video, driven over the Chrome DevTools Protocol without an extension in Antigravity 2.0 (<https://antigravity.google/docs/browser-subagent>), and Google publishes an AGY CLI UI-testing codelab combining it with Playwright (<https://codelabs.developers.google.com/agentic-ui-automation-with-antigravity>); Cursor ships an official Browser tool over CDP exposed to the agent as MCP tools, which stops at logins and long-running tasks (<https://cursor.com/docs/agent/tools/browser>); any agent CLI with shell access can also drive Playwright directly or through the Playwright MCP server (<https://github.com/microsoft/playwright-mcp>), which the harness `playwright-setup` skill installs and audits | owner: isolated General AI branch | class: MAIN.
- [x] Q2 — Answer how to run Claude Code on ChatGPT/OpenAI models | evidence: Anthropic's gateway documentation states it "doesn't support routing Claude Code to non-Claude models through any gateway" (<https://code.claude.com/docs/en/llm-gateway>), so the only route is a local Anthropic-format translating proxy: LiteLLM's documented setup runs `litellm --config config.yaml` with an `openai/` model entry, then `ANTHROPIC_BASE_URL=http://0.0.0.0:4000`, `ANTHROPIC_AUTH_TOKEN=$LITELLM_MASTER_KEY`, and `claude --model gpt-4o` (<https://docs.litellm.ai/docs/tutorials/claude_non_anthropic_models>); it bills an OpenAI API key per token rather than a ChatGPT subscription, and a gateway credential replaces the claude.ai subscription for that session | owner: isolated General AI branch | class: MAIN.
- [x] Q3 — Search broadly for working ways to run a ChatGPT subscription on the Claude Code harness or GUI | evidence: at least eight independent local Anthropic-format proxies reuse Codex CLI OAuth rather than an OpenAI API key — raine/claude-code-proxy, fcakyon/claude-code-with-codex, insightflo/chatgpt-codex-proxy, aryan877/claude-proxy, pilc80/claudegpt, Securiteru/codex-openai-proxy, CaddyGlow/ccproxy-api, and router-for-me/CLIProxyAPI; they pass OpenAI's auth check by reshaping requests into the Codex CLI form, which is unsupported and revocable. Anthropic's docs confirm the CLI, VS Code extension, GitHub Actions, and Agent SDK read `ANTHROPIC_BASE_URL`, the desktop app routes only through its third-party inference form and loses Remote Control and cloud environments, and Claude Code on the web and in Slack always use Anthropic's API (<https://code.claude.com/docs/en/llm-gateway-connect>). Anthropic prohibited subscription OAuth in third-party tools on 2026-02-20 and turned on billing enforcement on 2026-04-04; the reverse direction is governed by OpenAI and remains unenforced but unsupported | owner: isolated General AI branch | class: MAIN.
- [x] Q4 — Narrow the proxy field to one recommendation for running the Claude and Codex subscriptions together, and separate GUI from CLI support | evidence: `fcakyon/claude-code-with-codex` is the only surveyed proxy that relays `claude-*` straight to `api.anthropic.com` untouched, stores no Claude credentials, preserves prompt caching, and switches provider mid-conversation with `/model`, so both subscriptions serve one session; `aryan877/claude-proxy` reaches Anthropic only through an API key and `raine/claude-code-proxy` documents `ANTHROPIC_AUTH_TOKEN`, which Anthropic's docs say replaces the claude.ai login for that session, so neither keeps the Claude subscription alive. Surfaces: the CLI, VS Code extension, GitHub Actions, and Agent SDK read `ANTHROPIC_BASE_URL`; Claude Desktop on 3P takes an arbitrary base URL only through Developer -> Configure Third-Party Inference with `inferenceGatewayBaseUrl`, needs explicit `inferenceModels` because discovery filters to recognizably-Claude ids, and loses Remote Control, SSH sessions, and cloud environments; Claude Code on the web and in Slack always use Anthropic's API (<https://claude.com/docs/third-party/claude-desktop/gateway>, <https://code.claude.com/docs/en/llm-gateway-connect>) | owner: isolated General AI branch | class: MAIN.
- [x] Q5 — Research whether the Claude Code desktop GUI can serve a Claude subscription and a Codex subscription together | evidence: no, not in one session. Third-party inference is a deployment mode of the same app whose architecture table gives 3P sessions "Local device identity only" and no Anthropic account, so the claude.ai subscription is unreachable while it is active (<https://claude.com/docs/third-party/claude-desktop/overview>). It is a per-launch toggle instead: Help -> Troubleshooting -> Enable Developer Mode, Developer -> Configure Third-Party Inference, Apply locally, and "To return to standard Claude Desktop, choose the Anthropic sign-in option on the sign-in screen instead" (<https://claude.com/docs/third-party/claude-desktop/installation>), which contradicts open issue <https://github.com/anthropics/claude-code/issues/56606> claiming no documented path back and users deleting `%LOCALAPPDATA%\Claude-3p`. Loopback `http://127.0.0.1` base URLs are accepted while non-localhost http is rejected, and the picker filters to recognizably-Claude ids so `inferenceModels` or name mapping is required. This machine has `%LOCALAPPDATA%\Claude-3p` present and no `Policies\Claude` keys, so the in-app window is editable | owner: isolated General AI branch | class: MAIN.
- [x] Q6 — Write the Claude-plus-Codex subscription findings into the vault as a brief | evidence: pre-write sequence completed against the active vault resolved from the Obsidian registry (`C:\Users\dougl\Main\Yoga 7 Local John 1412`), `IA.md` sections 1 and 2, and the `40 Agentic Engineering` README; searched the folder for an existing owner and found none for the subscription/proxy subject, and the nearest note `Models & Delegation\Model Delegation Guide.md` is `authored_by: human` so it was not rewritten. Created `40 Agentic Engineering\Models & Delegation\Running Claude and Codex Subscriptions in Claude Code.md` and linked it from the agent-authored `Claude\Agent Orchestration & Coding Tools.md` in the same unit | owner: isolated General AI branch | class: MAIN.
- [x] Q7 — Recon the ChatGPT/Codex desktop app resume lag | evidence: root cause matched to `openai/codex` discussion 29949, which measures 26 WMI PowerShell launches per minute, 2,363 `git.exe` records in 65 seconds, and a ~7 s cold thread resume with the main process working set going 233 MB to 1,058 MB; mirror of this machine found `[projects.'c:\users\dougl']` trusted as a project root in `~/.codex/config.toml` with 88 trusted project entries, 48 top-level repositories and 119 `.git` entries under `C:\Users\dougl\projects`, and a live process census of 24 `pwsh` at 2,968 MB, 27 `conhost`, and 6 `codex`; no empty `.git` directories at depth 3. Babysit mode, so nothing was changed and the shortlist was returned for Douglas to choose | owner: Douglas | class: MAIN.
- [x] T7 — Narrow the Codex trusted project roots and prune the stale trusted entries | evidence: backed up `~/.codex/config.toml` to `config.toml.bak-20260831-pretrustprune` (17,976 bytes), then removed 54 of 88 `[projects.*]` blocks — 51 ephemeral temp test directories, the `c:\users\dougl` home root, the `c:\users\dougl\projects` multi-repo parent, and `c:\tmp\aht-measure` — keeping 34 individual project roots that exist on disk; file went 475 to 304 lines, `tomllib` parses it with 19 top-level keys, 34 project entries, and `sandbox_mode = workspace-write` intact. Script at `scratchpad/prune_codex_trust.py`, dry-run by default. Resume timing is Douglas's to measure after restarting Codex Desktop | owner: isolated General AI branch | class: MAIN.
- [x] Q8 — Answer why slash-command options do not pop up in the Claude Code GUI | evidence: known desktop-app gap, not local configuration — 131 skills are present under `~/.claude/skills` and typing a full command name works. `anthropics/claude-code#40413` asked for desktop autocomplete and was auto-closed as a duplicate of `#10246`, which is a CLI/VS Code parity issue closed COMPLETED on 2025-10-24, so the desktop request was mis-triaged rather than fixed; `#21229` (Windows 11 desktop `/` and `@`), `#42038`, and `#32331` are all closed too. Mid-prompt `/` is plain text by design: `#55173` is closed NOT_PLANNED. Local CLI is 2.1.177; desktop builds app-1.37937.3 and app-1.40609.0 | owner: isolated General AI branch | class: MAIN.
- [x] T8 — Update every agent CLI on this machine | evidence: Claude Code 2.1.177 to 2.1.252, Codex CLI 0.145.0 to 0.151.0, opencode-ai 1.18.23 to 1.18.25, plus codeburn 0.9.23, vercel 59.10.0, and @modelcontextprotocol/server-filesystem 2026.8.31; Antigravity 1.1.22 and cursor-agent 2026.08.25-3e8eec8 already latest. Two defects found and fixed: `codex update` failed with `Get-FileHash` unrecognized because this session's `PSModulePath` was polluted, cured by resetting it to the machine value; and `C:\Users\dougl\bin\claude.exe` (245 MB, 2026-06-13) shadowed the managed install on PATH so every update was invisible — archived as `claude.exe.stale-2.1.177.bak` and `claude.cmd.stale-2.1.177.bak`, and `claude` now resolves to `C:\Users\dougl\.local\bin\claude.exe` at 2.1.252. `obsidian-mcp` 1.0.6 to 2.0.1 held: major bump that could break MCP wiring | owner: isolated General AI branch | class: MAIN.

## Verification

`powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$env:USERPROFILE\.agents\tools\Manage-Harness.ps1" -Action VerifyProject -Repository .`
