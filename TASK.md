# Task

## Goal

Finish repository-first cross-device reconstruction from the canonical harness and project GitHub authorities, preserve approved Obsidian configuration and declared external project data, retire or map legacy roots, and prove the complete flow on another computer.

## Active

- [ ] S0 — Social saved-post catalog: finish the Instagram AI/technology saved-post corpus under goal conditions C1–C6 | owner: this session (branch `claude/instagram-saved-catalog-29bc2e`) | class: MAIN | staging: `.local\social-catalog-20260919` under `C:\Users\dougl\Projects\general-ai` | corpus: `50 Knowledge\57 Corpus\Saved AI Posts`. The goal's conditions C1-C6 are tracked here as SC1-SC6, because C1-C6 were already taken by a completed 2026-07 cross-device item; SC7 carries the privacy invariant and SC8 the per-item judgement gap found 2026-09-19.
  - [ ] SC9 — Publish the nine topic syntheses the goal names, not only the topics the classifier happened to emit | baseline 2026-09-20: the builder's dry run produced 5 syntheses (ai-news, design-tools, agents-and-coding, research, workflows-and-productivity) because topic assignment came from the classifier's free-text topics; cad-and-3d, business, hardware and security have no note | done when each of the 192 members carries one `primary_topic` from the fixed nine, and every topic with at least one member gets a synthesis note | verifier: the primary_topic histogram sums to 192 and the published synthesis count equals the count of non-empty topics | class: SEQUENTIAL (follows SC8). <!-- agent: instagram-saved-catalog/session --> Progress 2026-09-20: `catalog-records-v3.json` carries one `primary_topic` per confirmed member and all nine topics are non-empty (agents-and-coding 38, design-tools 37, business 23, ai-news 19, cad-and-3d 18, hardware 15, unsorted 13, research 13, security 10, workflows-and-productivity 6), so the corpus owes ten syntheses including `unsorted`; the builder rebuild is dispatched to emit them. Rebuild landed 2026-09-20 with ten syntheses, but SC9 is NOT done: the `primary_topic` values the syntheses group by were found on inspection to be padded into the three smallest buckets ("Weekly AI news claims roundup" assigned `cad-and-3d`, "A week of fast-moving AI headlines" assigned `hardware`), so a reassignment packet writing `classification\primary-topics-v2.json` must land and the syntheses be regenerated from it before this closes. Overlay landed and verified 2026-09-20: 192 of 192 justifications quote four or more consecutive words of the member's own text, 192 distinct, 0 containing the stable id, 92 members changed topic, and `cad-and-3d` fell from 18 to 1 — the padding was real. Corrected 2026-09-20: the earlier `cad-and-3d` figure of 1 was read from the overlay mid-write; the final file gives cad-and-3d 9 and 86 changed members, with the histogram agents-and-coding 60, design-tools 37, workflows-and-productivity 18, business 12, security 12, research 12, ai-news 11, hardware 11, unsorted 10, cad-and-3d 9. Republish verified 2026-09-20: 192 notes match the overlay with 0 mismatches and all carry `primary_topic_rationale`, and 10 syntheses were emitted, one per non-empty topic. No topic fell below three members, so the thin-topic rule stands unused in this run. Reviewer reproduced the topic check 2026-09-20: 192 of 192 notes match the overlay, 0 mismatches. But it judged all ten syntheses to be link lists with a generic opening and no corpus-level argument, and found the `cad-and-3d` bucket incoherent — most of its nine members are general Claude or Fable showcases, not CAD or 3D. Round two 2026-09-20: the reviewer read all ten and found each carries a collective argument with contract cross-links, and that noun substitution would not turn any one into another topic's note — `TOPICS=10 ARGUMENTS=10 OWNER_CROSSLINKS=10`. One defect remains and is dispatched: the `cad-and-3d` coherence test omits the literal term `3D`, so its opening reports 2 of 9 where at least 7 of 9 members are genuine 3D or interactive-prototyping work. That is an understatement rather than the original padding, and the honest fix is to say the bucket is a real 3D cluster whose label is CAD-heavy. SC9 closes when that statistic matches its members.
  - [ ] SC3 — Bring `.local\build_social_publication.py` up to the twelve-point publication contract | baseline 2026-09-19: stages 213 notes with `vault_write: false`; emits `image: null` for every note, bare wikilinks, usefulness rating fused with rationale, no backup-before-overwrite, no human-authored refusal, no plan or geo_grandmasters handling, and silently repairs malformed DM JSON at load | done when a dry run proves all twelve contract points | verifier: a reviewer that did not write the builder runs the dry run and checks all twelve | class: PARALLEL. <!-- agent: instagram-saved-catalog/session --> Progress 2026-09-20: 11 of 12 points proven in `publication-dryrun/CONTRACT-EVIDENCE.md`; point 1 unproven because the official project image copy was not demonstrated. Open defect found by reading a generated note: list-valued unknowns are emitted as `["unresolved"]` for `topics`, `post_claims`, `scam_markers` and `risk_flags`, which makes a fake marker filterable in the Base — an unknown list must be `[]` with the unknown recorded in a status field instead. Rebuild landed 2026-09-20: 192 notes, 10 syntheses, zero `["unresolved"]` markers outside Backups, and contract point 1 proven by an embedded `Images/crucix-dashboard.png` captioned as an official project image rather than Instagram media. `CONTRACT-EVIDENCE.md` now claims twelve of twelve; the claim still owes the independent review by an agent that did not write the builder, which is what closes SC3. Review returned 2026-09-20 with verdict **not ready** and eight blocking findings, recorded at `qa\publication-review-20260919.md`: no structural vault-write gate, 21 `hype_evidence` nulls published as the string "unresolved", caption provenance mislabelled on all 192 notes, ten syntheses that are link lists, an incoherent `cad-and-3d` bucket, one note describing uncaptured media, a clean rerun missing the plan copy, and an unreproducible 810-wikilink claim against a measured 204. Round two 2026-09-20 at `qa\publication-review-2-20260920.md`: still **not ready**, but five of the eight are verified fixed by the reviewer's own commands and all twelve contract points now pass except point 4. Wikilinks settle at 254 over 206 non-backup Markdown files excluding `CONTRACT-EVIDENCE.md`, `Backups`, `_review` and `_evidence`. Five fixes remain and are dispatched: seven judgement values that still describe uncaptured media, fourteen values the body renderer rewords away from the batch, an evidence generator that is not location-invariant, a backup count of 208 against a manifest holding 207, and a `cad-and-3d` coherence test that omits the literal term `3D` and so understates 7 of 9 as 2 of 9. Point 4 closes with PyYAML 6.0.3 at the Python313 interpreter. SC3 closes only when a review returns ready.
  - [ ] SC4 — Publish a useful corpus | baseline 2026-09-19: `50 Knowledge\57 Corpus\Saved AI Posts` holds 1 index note and 1 note (geo_grandmasters); 0 `.base` files; 0 topic syntheses | done when there is one source note per accepted AI/tech post, one Obsidian Base filtering by topic, usefulness, verification status, risk, source type and enrichment state, topic syntheses across the nine named areas, official project images embedded and labelled as project images rather than Instagram media, and cross-links into existing vault notes with no human-authored file rewritten | class: SEQUENTIAL (after C1, C3). <!-- agent: instagram-saved-catalog/session -->
  - [ ] SC5 — Prove no validation regression | baseline measured 2026-09-19: `Verify-VaultLinks.ps1 -Scope "50 Knowledge"` = 680 notes, 5762 links, BROKEN 0, AMBIGUOUS 26, ORPHANS 26 | done when the same scoped run reports BROKEN 0 and AMBIGUOUS at most 26, `Invoke-VaultLint.ps1` passes at that scope, and every published note's YAML parses with an already-installed parser and no new dependency | class: SEQUENTIAL (after C4). <!-- agent: instagram-saved-catalog/session -->
  - [ ] SC6 — Correct and complete the continuation plan | baseline 2026-09-19: `plan/Continuation plan and progress map.md` wrongly reports `classification/ai-classification.json` and `browser/instagram-dm-ai-resources.json` as absent (both exist; the DM file was malformed JSON) and predates the authorization to publish | done when it carries corrected measured counts, queues, schemas, state transitions, stable-ID upsert rules, bounded browser batches, verification gates, conflict-free work division, the next ten packets, and a ready-to-paste unattended prompt | class: PARALLEL. <!-- agent: instagram-saved-catalog/session --> Progress 2026-09-20: revision 4 landed with revisions 2 and 3 preserved. It carries the privacy lesson and its standing rule that an invariant no gate tests is not enforced, the topic-overlay supersession, the anti-splice second clause, the reviewer-model constraint, credibility batch 005, the merged PR 1536, and the re-measured vault baseline. Its stale next-packets and done sections were rewritten to seven real packets, explicitly not padded. Two action-bearing errors about `geo_grandmasters` were corrected directly in the plan and in the staged publish brief. Remaining for SC6: the final reconciliation against the published state, which is packet 7 and cannot run before publication.
  - [ ] SC7 — Hold the privacy invariant across every condition: retain only AI/tech resources from DMs, never private message text, never message, follow, purchase, or submit a form | verifier: the published tree contains no DM sender, display name, handle, thread title, conversation timestamp or message body | baseline 2026-09-20: the verifier was too narrow and passed while the corpus published eight DM sender display names, because it tested only for message bodies; it now tests for senders and handles too | class: PARALLEL. <!-- agent: instagram-saved-catalog/session --> Progress 2026-09-20: violation found and fixed at source — the builder sourced the raw DM file and rendered `sender`; it now sources the handle-free `browser\dm-resource-index.json` and renders resource name, kind and URL only. Verified after rerun: 0 occurrences of the eight names across the tree, and the 12 backup copies that retained them were removed. Not yet closed: the same sweep must run against the vault-published tree after SC4, and the independent reviewer must reproduce it.

## Queue

- [ ] T4 — Prove the merged onboarding flow on the receiving computer | after: B1, B2, B4 | implementation owner: [harness issue 18](https://github.com/pyrgos-ai/doug-harness/issues/18) | evidence: a receipt tied to the merged harness commit proves install/stamp, approved Obsidian bundle, PATH/executables, GitHub discovery, declared project verifiers/adapters, Bitwarden token import/read-back, and current General AI, Agent Brain, and Docket access | owner: receiving computer | class: SEQUENTIAL.

## Blocked

- [!] B1 — Publish the current approved Obsidian bundle in canonical harness onboarding | owner: active harness owner | evidence required: `webviewer=true`, pinned `html-page-viewer@0.1.1` package/checksums, receiving-side backup and restore fixture, refreshed manifests, reviewed merge, and merged-master install.
- [!] B2 — Publish the exact mutable-runtime verifier repair | owner: active harness owner | evidence required: harness `master` accepts only `logs/stale-agent-process-cleanup.jsonl` and `state/permissions-snapshot.json`, rejects undeclared siblings, and passes focused plus installed verification.
- [!] B3 — Restore the consolidated task-state hook suite to green | owner: active harness owner | current evidence: `Test-AgentProjectState.test.ps1` passes; `task-state-reminder.test.js` passes 51 and fails 6 chain/PROMPT_LOG cases.
- [!] B4 — Move prerequisite discovery ahead of Obsidian and Project Data mutation | owner: active harness owner | evidence required: blank-profile onboarding returns a complete `ATTENTION` inventory before either dependent stage can fail, and a blank-profile regression passes.

## Needs decision

- None | owner: n/a — future cloud implementation, Agent Brain automation, and preserved OneDrive data retirement remain parked in `BACKBURNER.md`.

## Constraints

- Preserve `C:\Users\dougl\My Drive\Project Data`; do not delete, rename, or repurpose it.
- Keep the 14-repository cloud rollout plan-only through `pyrgos-ai/doug-harness` issue #16.
- Do not recreate Drive Capsule, Password Manager scaffolds, or the retired 3/4/3 retention policy.
- Do not touch `_backups`, `research\docket-briefs`, the preserved OneDrive tree, credential values, protected vault paths, or another task's worktree.
- This task owns only the isolated General AI access/state branch. The canonical harness-owner worktree remains read-only.

## Completed
- [x] SC11 — Check the highest-usefulness members' named resources against the world | baseline 2026-09-20: judgement rested on what each post says about itself; `credibility\batch-001..004.json` cover earlier rounds | done when `credibility\batch-005.json` records up to 25 previously-unchecked resources from `high`-rated confirmed members with resolves, exists_as_described, claim_vs_reality, gated and evidence_url, and an unrun check is recorded as `unchecked` rather than a pass | verifier: json.load succeeds and every gap is listed with its stable id | class: PARALLEL. <!-- agent: instagram-saved-catalog/session --> Done 2026-09-20: 25 resources checked. Resolves 24 yes / 1 no; exists_as_described 14 yes / 10 partial / 1 unchecked; gated 13 yes / 12 no; all 25 carry a specific claim-versus-reality gap and name what was not tested. Residual: the packet left one read-only Chrome tab open and reported it, and that tab is not reachable from this session.

- [x] SC8 — Give every collection member real per-item judgement | DONE 2026-09-20: `classification/judgement/batch-1..4.json` cover all 192 members, 48 each, 192 unique ids with zero cross-batch duplicates, every id a confirmed member, and all fifteen judgement keys present on every item. Anti-splice audit passes: 48 distinct titles and 48 distinct rationales in every batch, 44 to 48 distinct next actions, zero items splicing the shortcode into a title or rationale, zero titles that are a prefix of their caption, and `hype_assessment` holding only the four permitted enum tokens. The full usefulness range is in use and 27 items carry independent verification drawn from a real credibility file. Two earlier batches produced at `medium` were rejected as templated and remain quarantined as `rejected-batch-1-gamed.json` and `rejected-batch-3-gamed.json`, never to be merged | verifier: a reviewer samples 20 items and finds every factual claim backed by a named input file | class: PARALLEL. <!-- agent: instagram-saved-catalog/session -->
- [x] SC10 — Bound the Instagram DM scan honestly and publish only its AI/technology resources | DONE 2026-09-20: `browser/dm-scan-scope.md` records the scan date, what the pass covered (8 threads, 23 Instagram references, 1 external URL), what it demonstrably did not cover (older inbox rows still loading, destinations behind gated actions, and the posts left after a browser batch detached), and states that any published corpus must call the scan partial; `browser/dm-resource-index.json` carries 43 resources — 23 Instagram shortcodes, 18 named resources, 1 repository, 1 tool — with `scan_completeness: partial`, none of the 23 shortcodes being AI-collection members | evidence: grep of both files returns 0 `@` mentions and no message text, sender, display name or thread detail | class: PARALLEL. <!-- agent: instagram-saved-catalog/session -->
- [x] S1 — File harness defects found during this work as pull requests on owned branches | evidence: pyrgos-ai/doug-harness PR 1536 "Resolve forced surface model and default quota path" MERGED 2026-09-20 from `agent/force-surface-model-resolution`, fixing two measured defects — `Resolve-ProviderLadder.ps1` failing with `quota-state-unreadable` because `-QuotaStatePath` had no default, and `-ForceSurface` dispatching with an empty model and effort so the adapter refused caller-fault | class: PARALLEL. <!-- agent: instagram-saved-catalog/session -->
- [x] SC1 — Prove Instagram "AI" Saved collection membership | DONE 2026-09-19: collection "AI", id `18059768654711888`, url `https://www.instagram.com/douglaspmcgowan/saved/ai/18059768654711888/`, 192 members enumerated exhaustively over 16 API pages terminating on `more_available == false`, newest 2026-09-19T12:01:24Z, oldest 2026-02-26T05:55:00Z, per-shortcode set recorded in `browser/ai-collection-membership.json`; 141 members present in the export, 51 absent and newly captured, 405 export Instagram records outside the collection | remaining: stamping `collection_membership` onto every record is C2's output `classification/catalog-records-v2.json` | verifier: a fresh agent re-paginates the collection feed from the recorded route and matches 192 | class: SEQUENTIAL (gates SC3, SC4). <!-- agent: instagram-saved-catalog/session -->
- [x] SC2 — Make the 614-vs-546 provenance honest | DONE 2026-09-19: `classification/catalog-records-v2.json` carries 665 records — 597 Instagram (192 `confirmed`, 405 `rejected`) and 68 LinkedIn (`not_applicable`) — each with `source_platform`, `source_file`, `collection_membership` and `membership_evidence`; independently re-verified that the confirmed id set equals `browser/ai-collection-membership.json` exactly and that 30 of the 32 `checked` records are members | verifier: an independent platform-by-category recount matches every published number exactly | class: PARALLEL. <!-- agent: instagram-saved-catalog/session -->
- [x] T1 — Publish current phone and agent pickup context | evidence: General AI PR 5 merged at `cec979f`; the access index uses the stable Docket card `agent-harness--cross-device-access-index-2026-07-31`; Agent Brain PR 3 merged the constrained refresh proposal at `fd584d1`; later transport updates preserve the same three-surface publication contract.
- [x] T2 — Reconcile durable General AI state with current authorities | evidence: the closeout reconciles TASK, STATUS, LOG, machine context, and provenance against General AI `cec979f`, Agent Brain `fd584d1`, the 174-card Docket store, and the current vault copy; project verification and independent review pass.
- [x] T5 — Publish one value-safe receiving-device receipt schema and refresh the cross-device access index | evidence: PR 10 merged at `84f1cf6`; the schema compiles as JSON Schema 2020-12, accepts one positive fixture, and rejects twelve negative fixtures covering credential fields in the Bitwarden and harness sections, retention above five snapshots, a SQLite adapter missing its retention block, a retention block on a non-SQLite adapter, `preserveNewestVerified` false, a duplicated access surface, a missing fourth surface, an abbreviated harness commit, an unknown top-level section, a wrong onboarding entry, and a wrong Bitwarden import variable; Gitleaks 8.30.1 found no leaks; the access-index body matches across Git, the active vault, and Docket at SHA-256 `a81eae1b7f98e5d00b16eba447693f67f0c35bc35e34ec7407c7ed680d93621e`; the brokered publication pushed 173 cards and refused 0 | owner: isolated General AI branch | class: MAIN.
- [x] T6 — Close the phone-safe entrypoint gaps found by the read-only link audit | evidence: all 15 access-index links resolve, with every private 404 confirmed as an auth gate over a path that exists on its default branch; the index now links `START-HERE.md`, both secret manifests, `recovery/`, `STATUS.md`, `TASK.md`, `MAP.md`, `MIGRATION.md`, both project manifests, and the BitLocker readiness brief, and adds a lost-private-access fallback; the two entrypoints now agree that the v2 machine-readable context is current; the vault START HERE brief was rewritten with absolute URLs so it works off-repository | owner: isolated General AI branch | class: MAIN.
- [x] T3 — Resolve every attention-required project Git transport | evidence: Boundaries Reader plus Bible Name Search, Contact Form Caller, Drive Organizer, Grandpa Help, and Motion to Dismiss have reviewed portable baselines on their remote default branches with `agent-project`, green latest hosted Gitleaks scans, complete declared Git-backed skill bundles, and preserved fail-closed local hooks; `general-claude`, `harness`, and `idetc-writing-ide` retain explicit consolidation mappings; remaining project-data and licensing attention is declared separately in the fleet inventory | owner: project fleet | class: PARALLEL.
- [x] C1 — Replace Drive Capsule distribution with the canonical repository root `ONBOARDING` flow.
- [x] C2 — Adopt SQLite retention of 2 daily, 2 weekly, and 1 monthly bucket, always preserving the newest verified snapshot and no more than five distinct snapshots.
- [x] C3 — Preserve completed cloud, retrospective, second-brain, and graph-planning research in GitHub, the active Obsidian vault, and Docket.
- [x] C4 — Publish the phone-safe access index and pickup context through General AI PR 3 at `9fa51b6`.
- [x] C5 — Publish the portable Agent Brain contract through private mirror PR 2 at `337d615`.
- [x] C6 — Publish the 27-root portability inventory through General AI PR 4 at `340a05f`.

## Verification

Social catalog next verifier: Verify-VaultLinks.ps1 -Scope '50 Knowledge' -WarnOnly (baseline 2026-09-19: 680 notes, 5762 links, BROKEN 0, AMBIGUOUS 26, ORPHANS 26; pass requires BROKEN 0 and AMBIGUOUS <= 26), then Invoke-VaultLint.ps1 at the same scope.

`powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$env:USERPROFILE\.agents\tools\Manage-Harness.ps1" -Action VerifyProject -Repository .`
