# Current Task

## Goal

Establish one canonical cross-agent global harness and a reliable intake-to-ledger task system while finishing the remaining Bitwarden, Drive, hook, Capsule, and documentation work without overwriting Douglas's live Codex rules.

## Completed

- Installed the first guarded stale-agent-process cleanup and recurring task.
- Published the verified Capsule under Google Drive My Drive.
- Built and live-verified the guarded `declog` skill across Chrome/Playwright, Node, Python, WebView2, and agent helpers.
- Consolidated the human-readable harness guides and deployed matching live/private copies.
- Built value-safe Bitwarden Login scaffolds with empty Hidden fields and regression tests.
- Repaired and verified the nightly backup task registration.
- Reproduced the active latency condition and isolated Google Drive broad sync as the largest measured load.
- Audited the current Codex rules, task-state hooks, memory paths, frontend testing, Superpowers routing, and code-graph options.
- Re-intook the outstanding work with `source-command-task` and ran three independent parallel audits.
- Installed signature-verified Microsoft RAMMap v1.63 and measured the kernel pools over a 30-second trace.
- Audited Chrome's process tree and isolated two high-memory tasks for user-visible identification.
- Recorded the premature-completion/under-parallelization correction in the shared feedback log.
- Merged private harness PR 2 and fast-forwarded the local harness authority.
- Researched vendor, academic, practitioner, and local evidence for automatic task capture, dependency tracking, agent-created subtasks, durable state, resume, and evidence-gated completion.
- Designed the intent-ledger plus task-graph lifecycle and its cross-product hook, schema, projection, and fixture requirements.
- Staged and independently verified the concise global `AGENTS.md`, universal `DESIGN.md`, global `MAP.md`, lean `MEMORY.md`, project starters, product adapters, baseline skill map, `/correct`, and management verifier.
- Staged and independently verified the three-dispatcher hook system, bounded `TASK.md` migration, lifecycle telemetry, notifications, usage-limit handling, and fail-closed preflight security.
- Staged and independently verified the Docket all-personal-card cloud policy, single-secret Bitwarden mapping, portable Capsule entrypoint, path-containment fixes, and alternate-profile restore.
- Consolidated the human-readable harness into one Markdown guide plus a browser-tested HTML mirror with two rendered diagrams and source-hash parity.
- Re-ran the assembled three-dispatcher hook suite, alternate-profile Capsule backup/restore, Bitwarden scaffold/wrapper tests, and Docket patch applicability after the latest fixes; all passed.

## Remaining

1. Resolve the portable-skill path/provider findings, Superpowers routing gap, correction-secret guard, and final adversarial review findings.
2. Back up and deploy the verified global package, product projections, three hook dispatchers, project starter/migration, Docket patch, Capsule patch, and human guide.
3. Verify Git state under the interactive Windows identity, commit/push the authorized repositories, refresh `My Drive\Capsule`, restart Google Drive, and confirm upload.
4. Run the interactive Bitwarden login/unlock wrapper; Douglas supplies the master password and emailed OTP, then fills the empty Hidden values.
5. Run the live cross-product, hook, task, link, HTML, credential-safety, Docket, backup, and restore verification suite.
6. Keep the RAMMap elevation capture parked until the harness deployment is complete.

## Next verifier

Run the global manager, correction, HTML, skill-path audit, and full staged verification after the parallel fixes merge; then perform the final permission-gated deployment.
