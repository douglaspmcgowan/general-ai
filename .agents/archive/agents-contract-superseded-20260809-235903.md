# Sections removed from AGENTS.md when the managed block absorbed them

Removed from `C:\Users\dougl\Projects\general-ai\AGENTS.md`. Each heading below now lives inside the managed block, and the
text kept here is what the project carried outside it, which differed. Nothing here is
read by any tool; it exists so the removal is recoverable.

## Start and resume

1. Read this file.
2. Read `TASK.md` and recent entries in `LOG.md`.
3. Run `git status --short --branch` and `git worktree list --porcelain`.
4. Read `MAP.md` for architecture, data, ownership, integrations, or important paths.
5. Read `DESIGN.md` for interface work and `PRODUCT.md` when present.
6. Reconcile inherited claims against files and Git before editing.
