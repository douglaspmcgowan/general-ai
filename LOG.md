2026-07-24 | /claude-sync full run: pushed protect-ai-reference 4-rule superset + unioned secret tokens + merged claude-sync skill to origin (4ba4be0); pulled down 61-commit backlog (skills 24->46, commands 92->108, hooks 57->60, 45 files path-rewritten); wired dep-audit-gate + test-green-gate; retired repo-map.md; scrubbed Zahra scheduled task + hub memory; sites hub verified in sync.
2026-07-26 | Built and verified the cross-agent Setup system: shared contracts, session/handoff guidance, project bootstrap, SQLite 3.53.4, Gitleaks 8.30.1, Bitwarden broker hardening, Cursor state/security adapters, Google Drive and Device Encryption briefs, integrity stamping, and Obsidian publication.
2026-07-27 | Completed the cross-agent harness adversarial pass: verified 21 repository baselines, hardened four-field Bitwarden command policy, serialized Skills Docket generation, passed the 93-file setup verifier, and recorded the final external boundaries.
2026-07-27 | Published the private harness and Docket repositories, installed Docket startup, authenticated GitHub CLI, removed OneDrive, and established the 94-file human-readable harness layer.
2026-07-27 | Migrated and exactly verified ten repositories plus the Flight Tracker coordination folder under the lowercase project root; created the lean general-claude coordination repository and preserved active 168 Audit and Berkeley House worktrees for owner handoff.
2026-07-27 | Repaired Docket loopback authentication with 73 passing tests, imported five setup handoffs, refreshed the human-readable harness, and preserved the IDETC archive in its per-project data folder.
2026-07-27 | Reauthorized GitHub CLI in the Windows keyring, configured reusable Git HTTPS credentials, pushed Docket and harness updates, and refreshed the remote-verified recovery pointer.
2026-07-27 | Queried Google Drive's root-preference database read-only and confirmed nine unsafe broad selected folders; kept the client stopped and documented the exact cleanup.
2026-07-27 | Moved the 168 main repository into lowercase projects, created verified replacement 168 and Berkeley worktrees under Worktrees, and retained ACL-locked old paths as rollback copies.
2026-07-27 | Copied missing personal files out of OneDrive without overwriting local collisions, verified no missing-file work remains, and stopped Google Drive after it auto-restarted with the same nine unsafe backup roots.
2026-07-27 | Installed and live-verified nightly portable Agent Backups, three-mode project restore, approved application-data recovery, full-tuple Bitwarden brokering, local known-folder repair, and deterministic 13-folder Quick Access recovery.
2026-07-27 | Built the 25-project Capsule, renamed the coordination authority to general-ai, added clean-clone-proven offline bundles and UTF-8-safe restore, and passed a full disposable computer rebuild.
2026-07-28 | Made Capsule self-explanatory for agents and humans, documented cross-computer updates and retention, expanded Bitwarden/account guidance, confirmed the Google Drive Computers retrieval route, and added Codex renderer process-safety enforcement.
2026-07-28 | Reproduced the sandbox-versus-desktop authentication split, reused the existing GitHub keyring login, preserved receiving-computer OneDrive during Capsule staging, refreshed every Capsule copy, and audited Windows memory accounting.
2026-07-28 | Installed a regression-tested guarded stale-agent cleanup task, published the verified 5,740-file Capsule under Google Drive My Drive, added the portable remaining-steps checklist, and reduced open work to four human-gated actions.
2026-07-29 | Built and verified the portable declog skill, Bitwarden value-safe scaffolds, repaired backup-task installer, and consolidated the human-readable harness into an 118-file stamped deployment.
2026-07-29 | Reproduced severe latency under broad Google Drive sync, reduced measured CPU and disk pressure by stopping Drive, audited Codex rule bloat and stale paths, and recorded the remaining Bitwarden, Drive, hook-governance, and publication gates.
2026-07-29 | Refreshed and integrity-verified the Documents and My Drive Capsule copies from successful nightly snapshot 20260729-002722; cloud upload remains gated on narrowing Google Drive Preferences and restarting Drive.
2026-07-29 | Audited 39 Codex hook invocations, designed a four-dispatcher reduction to 10 commands, verified the three unsafe Google Drive roots remain active, and published the consolidated harness as private draft PR 2 at commit 8309800.
2026-07-29 | Re-intook the full queue with source-command-task, dispatched three independent audits, installed signature-verified RAMMap v1.63, measured flat 4 GB kernel pools, isolated Chrome's two largest trim candidates, and recorded the premature-completion correction.
2026-07-29 | Researched automatic agent task management across current vendor, academic, practitioner, and local evidence; designed an intent-ledger/task-graph lifecycle with governed self-created subtasks, generated human views, and evidence-gated completion; reconciled the active project state.
2026-07-29 | Task state consolidated into TASK.md; legacy task and verification sources retained under .agents/archive/task-state-migration.
2026-07-29 | Reconciled newer task, status, log, and task-management research into canonical general-ai; migrated legacy task state into TASK.md; installed the v3 project contract; and verified the project while preserving both source generations under .agents/archive.
2026-07-29 | Closed the cross-agent rollout: merged harness PRs #2 (`5245929e362fddf912f6f43dd2efc3b141241fb4`), #3 (`0435bb8c38e09d5d9a18c46c11d7d9a1bbdf6a63`), #4 (`03c0f0d8c2692dbbaa13411d1c5ff7379f9aa589`), and #5 (`727662d4881eeadcef605f15cb268d1791774ad3`); merged Docket PR #1 (`2489f7a32617eec287cf36d9a521636cc38b01f9`); installed the three-dispatcher harness and consolidated human guide under `C:\Users\dougl\.agents`; launched Google Drive; and refreshed `C:\Users\dougl\My Drive\Capsule` to snapshot `20260729-065807`. Bitwarden interactive authentication/value entry and an optional elevated RAMMap capture remain.
2026-07-29 | Merged harness PR #6 at `0d19eeca14bc0313705cf57390e18c0f822e75a5` and refreshed `C:\Users\dougl\My Drive\Capsule` to snapshot `20260729-071541` with 28,462 packaged files.
2026-07-29 | Reconciled the human guide, HTML mirror, and Capsule onboarding with the verified final rollout; replaced stale snapshot, hook, task-migration, Docket-patch, and Documents-Capsule claims; and passed the harness, Capsule lifecycle, and portability suites.
2026-07-29 | Repointed the redline, drive-organizer, and contact-form-caller Claude launch entries from OneDrive rollback copies to their canonical project roots; parsed the configuration and passed three PID-scoped HTTP smoke tests.
2026-07-29 | Merged Docket PR #2 (`32d2346`) and status commit `9aa727a`; backed up the local store, archived the two stale setup handoffs by exact ID, and reconciled 162 local items to 160 unresolved outbound cards.
2026-07-29 | Verified signed RAMMap v1.63 at its canonical executable, recorded a 95%-physical/4.02-GiB-pools pressure sample, and preserved the failed-to-write elevated snapshot plus optional RAMMap/WSAIFabric follow-up as an explicit decision.
2026-07-29 | Fixed packaged Capsule sync in harness commit `6840e6a`, installed and verified the corrected global harness while preserving the active Codex hash, and deployed verified snapshot `20260729-081324` with 34,062 integrity records; the packaged refresh tool and account map passed lifecycle checks, Gitleaks scanned roughly 743.55 MB with no leaks, and Google Drive is running.
2026-07-29 | Reconciled active project state with the integrated long-path Capsule cleanup, verified OneDrive application retirement while preserving its local data tree and registered legacy worktrees for an exact retention decision, and recorded the in-progress 3/4/3 policy plus final install, Capsule refresh, push, and credential gates.
2026-07-29 | Recorded the successful 8-item Bitwarden scaffold, pushed Docket and harness release commits, installed and verified the global harness with the Codex hash preserved, froze the transitional Capsule, and adopted GitHub topic discovery plus per-project data-manifest transport for the 27-repository fleet.
2026-07-29 | Preserved every committed general-claude ref in recovery/general-claude-history.bundle at source master f8c1f39, verified complete bundle and clean clone integrity, and retained the original repository with its current working-tree state unchanged.
2026-07-31 | Reconciled general-ai with the repository-first harness: retired active Drive Capsule, Password Manager, and 3/4/3 claims; preserved My Drive\Project Data and the OneDrive decision boundary; recorded the plan-only cloud rollout under issue #16; and added the completed second-brain and graph-planning research to Git.
2026-07-31 | Recorded coordinated publication evidence: the four second-brain and graph-planning documents were mirrored to the active Obsidian vault, and six stable cloud, retrospective, second-brain, and graph-planning Docket cards were imported, broker-published, and verified by ID in the 166-card synchronization.
2026-07-31 | Reconciled General AI state onto current master, refreshed the phone and machine pickup entrypoints for the merged portability inventory and Agent Brain contract, and recorded the remaining harness, project-remote, data-adapter, and real-device proof gates.
2026-07-31 | Closed the current access publication loop: General AI PR 5 merged at `cec979f`; the Git, active-vault, and Docket access index matched at SHA-256 `22e944d6b223c8a02df501b3db416c73e431c182644f68a40bf4bf4167dcb3c8`; Docket pushed 172 cards with 0 unsafe or invalid local refusals; and Agent Brain PR 3 merged the constrained mirror-refresh proposal at `fd584d1`.
2026-07-31 | Reduced project transport attention actions from nine to five: merged the Boundaries Reader portable baseline at `53df3f7`, mapped three consolidated local roots to their Git authorities, created five private project remotes, preserved their unpushed migrations, and stopped at the shared pre-commit hook-composition guard.
2026-07-31 | Added harness issue 17 as the single implementation owner for a fail-closed, opt-in, backup-preserving migration from the five identical fixed-path Gitleaks hooks to the portable PATH-resolved template.
2026-07-31 | Added harness issue 18 as the consolidated owner for the approved Obsidian package, mutable-runtime verifier allowance, blank-profile ordering, integrated release checks, and real receiving-computer receipt.
2026-07-31 | Published reviewed portable baselines for Bible Name Search, Contact Form Caller, Drive Organizer, Grandpa Help, and Motion to Dismiss on private default branches with `agent-project`, complete declared Git-backed skill bundles, green latest hosted Gitleaks scans, and unchanged fail-closed local hooks; refreshed the 27-root fleet inventory to 20 clone actions, 20 unique discovery repositories mapped from 21 local roots, 10 clone-ready full remote baselines, and 17 placeholder data manifests.
2026-07-31 | Added a value-safe twelve-section receiving-device receipt schema, removed an ambiguous duplicate harness-commit field, linked it from the phone-safe access index and machine contexts, and separated the historical portability-inventory commit from live `master` schema ownership.
2026-07-31 | Merged the receipt-schema branch as PR 10 at `84f1cf6` after independent re-verification: JSON Schema 2020-12 compilation, one positive fixture, and twelve negative fixtures covering credential fields in two sections, over-cap and misplaced SQLite retention, `preserveNewestVerified` false, duplicate and missing access surfaces, an abbreviated harness commit, an unknown top-level section, a wrong onboarding entry, and a wrong Bitwarden variable.
2026-07-31 | Refreshed the phone pickup path: the active-vault access index now hash-matches merged Git at `a81eae1b…621e`, the vault START HERE brief was rewritten with absolute links and a surfaces table so it works off-repository, the receipt-schema explainer was added to `research/`, and Docket published 173 cards with 0 refusals across a 175-card store.
2026-07-31 | Corrected the overstated Engineer-brief Git-coverage claim to five of thirteen briefs having no published remote copy, and routed the full local-only context audit to the active vault and Docket rather than this public repository because it enumerates client and business repositories with their branch and working-tree state.
2026-07-31 | Preserved `recovery\general-claude-history.bundle` at source commit `f8c1f39ed369d8694eb9f82c50f622883bf29a7e`, and published the completed Obsidian second-brain and graph-aware agent-planning research under `research\`, mirrored to the active Obsidian vault and to Docket.
2026-07-31 | Merged General AI PR 9 at `60a3107`, recording the receiving-release owner; historical PR 7 at `582daf5` remains the source audit for the 27-root inventory.
2026-07-31 | Published reviewed `master` baselines with `agent-project` discovery topics, complete Git-backed skill bundles, green latest hosted Gitleaks scans, and unchanged fail-closed local hooks for the five formerly branchless private project repositories.
2026-07-31 | Merged private Agent Brain PR 3 at `fd584d1`, adding portable Claude/Codex loaders, map/spec/schema/state, fixtures, constrained `proposals/` and `agent-notes/` write zones, and the reviewed cross-device index refresh proposal.
2026-07-31 | Enabled Web Viewer and installed `html-page-viewer@0.1.1` on the live Obsidian bundle.
2026-07-31 | Merged General AI PR 10 at `84f1cf6`, adding `handoffs\RECEIVING-DEVICE-RECEIPT.schema.json` (a value-safe twelve-section receiving-device receipt) that compiled as JSON Schema 2020-12, accepted one positive fixture, and rejected twelve negative fixtures.
2026-07-31 | Ran a read-only audit confirming all 15 access-index links resolve: eight private links returned 404 anonymously as an authentication gate over paths verified to exist on their default branches, all public links returned HTTP 200, and the Docket surface returned 200.
2026-07-31 | Expanded the access index to also link the provisioning and recovery set (`START-HERE.md`, both secret manifests, `recovery/`, `STATUS.md`, `TASK.md`, `MAP.md`, `MIGRATION.md`, both project manifests, and the BitLocker readiness brief) with a fallback for unavailable private-repository access.
2026-07-31 | Rewrote the vault brief `Claude/Engineer/START HERE - Cross-Device Harness.md` with absolute URLs and a cross-device surfaces table, and preserved recoverable pre-change copies of both refreshed vault briefs under `Claude/Engineer/_backups/`.
2026-07-31 | Docket's most recent brokered publication pushed 173 of 175 local cards, refusing 0 unsafe or invalid cards.
2026-09-19 | Enumerated the Instagram Saved collection named "AI" exhaustively through the private collection feed API (16 pages, terminated on `more_available == false`): id `18059768654711888`, 192 members, newest 2026-09-19T12:01:24Z, oldest 2026-02-26T05:55:00Z, recorded with per-member evidence in `.local\social-catalog-20260919\browser\ai-collection-membership.json`. No save, unsave, like, follow, message or account change was performed.
2026-09-19 | Reconciled the 192 members against the 546-record historical export: 141 members are in the export, 51 are absent and were newly captured to `browser\ai-collection-newly-captured.json`, and 405 export Instagram records fall outside the collection. The classifier disagrees with Douglas's own curation on 44 members (20 labelled `non-ai`, 24 `uncertain`) and would have admitted 51 non-members, so the project's inclusion rule was inverted: membership decides inclusion, classification only supplies topic and priority.
2026-09-19 | Rewrote `.local\social-catalog-20260919\plan\Continuation plan and progress map.md` as revision 2 against the membership finding, adding the `social-catalog-item-v2` schema, the state model, the seven distinctions the corpus must keep separate, queues Q1-Q7 with measured populations, the twelve-point publication contract (five passing as measured), lane ownership, the next ten work packets, and a ready-to-paste unattended prompt.
2026-09-19 | Measured enrichment coverage against the 192 members: 30 carry a credibility assessment, 1 carries a browser enrichment, and of the three official project images exactly one (`crucix-dashboard.png`) maps to a member; the two Backflip images map to a LinkedIn post and therefore to no member. Repaired the malformed DM scan JSON (trailing comma, `.bak` kept); it now parses with 8 threads.
2026-09-19 | Re-measured the pre-publish vault baseline myself with `Verify-VaultLinks.ps1 -Scope "50 Knowledge" -WarnOnly`: NOTES=680, LINKS=5762, BROKEN=0, AMBIGUOUS=26, ORPHANS=26, UNREFERENCED_OUTPUTS=0, DUPLICATE_ROOTS=0. The post-publish comparison is therefore against an executed measurement rather than an inherited claim.
2026-09-19 | Rejected two per-item judgement batches that passed every mechanical check and were worthless: titles were caption prefixes with the shortcode appended so all 48 were "distinct", rationales followed one sentence template with the shortcode spliced in for the same reason, `what_to_do_with_it` had 4 distinct values across 48 items, `hype_assessment` fused the enum with its evidence quote, and all 48 items claimed `unreviewed` verification without any credibility file being opened. Quarantined as `judgement/rejected-batch-1-gamed.json` and `rejected-batch-3-gamed.json`, stopped the two in-flight slices, and rewrote the brief with anti-splice verification checks that a spliced answer cannot pass.
2026-09-19 | Measured the caption substrate behind the 192 members: 143 carry substantive text, 22 are one-line taglines, and 27 have only the export's auto-generated `Photo by <handle> on <date>.` fallback, which is not a caption and says nothing about the post. Those 27 are now a distinct content kind that must be rated `unresolved` and queued for browser capture rather than judged.
2026-09-19 | Opened pyrgos-ai/doug-harness PR 1536 on `agent/force-surface-model-resolution`, fixing two measured fleet-router defects: `Resolve-ProviderLadder.ps1` returned `quota-state-unreadable` on a bare invocation because `-QuotaStatePath` had no default, and the `-ForceSurface` escape hatch dispatched with an empty model and effort so the codex adapter refused caller-fault, turning the documented workaround for a quota lockout into a second dead end.
2026-09-19 | Hit a genuine constraint on the judgement work: the active usage mode excludes Claude models from the builder role (`mode-excluded-for-role:claude-pilots-codex`), `gpt-6-astra` is only reachable on an ineligible remote-desktop surface, and `muse-spark-1.3` is blocked by an opencode lane cap. The remaining route for a judgement-heavy packet is `gpt-5.6-luna` at `xhigh`, now under trial on one slice before any fan-out.
2026-09-19 | Captured the full, untruncated caption for all 192 AI-collection members straight from Instagram's collection feed, verified the id set matches the membership file exactly, and wrote them to `browser/ai-collection-captions-raw.json` (119,120 characters of caption text, shortest 11, longest 2,102). This retires the "27 placeholder posts" problem: the `Photo by <handle> on <date>.` lines were an artifact of the historical export, not what the posts say. The export's captions are now superseded as a source.
2026-09-19 | Moved 140 KB of captured data from the browser to disk without routing it through the session by opening a loopback sink page from the Instagram tab and using `postMessage` across origins, because an HTTPS page cannot `fetch` or clipboard-write to `http://127.0.0.1`. Both browser tabs were closed afterwards and the sink process stopped.
2026-09-19 | Publication builder rewrite returned 11 of 12 contract points proven against a dry run into `publication-dryrun`: 192 source notes one per confirmed member, zero bare wikilinks, 192/192 separate `usefulness_rating` and `usefulness_rationale` keys, strict JSON loading with the silent trailing-comma repair removed, a backup-and-refusal fixture, and a membership gate recording confirmed 192 / rejected 405 / not_applicable 68 with `vault_write: false`. Point 1 stands unproven because the official project image copy was not independently demonstrated.
2026-09-20 | Bounded the partial Instagram DM scan rather than extending it: `browser/dm-scan-scope.md` states what the 2026-09-19 pass covered, what it demonstrably did not (older inbox rows still loading, destinations behind gated actions, and the posts left after a browser batch detached), and that any published corpus must describe it as partial. `browser/dm-resource-index.json` distils it to 43 AI/technology resources — 23 Instagram shortcodes, 18 named resources, one repository, one tool — none of the shortcodes being AI-collection members. Both files verified to contain zero handles, display names, thread details or message text. No browser was opened and no message was read for this packet.
2026-09-20 | Brought the continuation plan to revision 3 against measured facts, preserving revision 2 alongside it: the full-caption capture supersedes the export and the 520-character truncation, the twelve-point contract table now reflects 11 passing and 1 unproven, the nine fixed primary topics replace the classifier's free-text topics, and a new "What is genuinely done, and what is not" section refuses to count the harness pull request as locally proven. It also carries a standing rule earned tonight: a distinctness check is satisfiable by splicing an identifier, so every per-item judgement brief must assert that distinctness derived from the identifier or source text is a failure and must include the anti-splice checks.
2026-09-20 | Fixed a contradictory privacy rule the plan revision introduced. One line permitted retaining a direct message's "URL, its sender handle, and whether it is gated" while the rule below it forbids recording senders or handles at all; the looser line would have licensed a future agent to write sender handles into the vault. Replaced it with the strict form and added the tie-breaker that where two statements of the privacy rule differ, the stricter one governs. The already-built DM index was produced under the strict rule and contains zero handles, so nothing required undoing.
2026-09-20 | Judgement batches 1, 3 and 4 passed the anti-splice audit against the full captions: 48 distinct titles and 48 distinct rationales each, 46/45/48 distinct next actions, zero items splicing the shortcode into a title or rationale, zero titles that are a caption prefix, the hype field holding exactly one of four enum tokens, the full usefulness range in use, and 0, 8 and 9 items respectively carrying independent verification drawn from a real credibility file.
2026-09-20 | Completed per-item judgement for all 192 collection members. Batch 2 closed the set: 48 items, ids matching its slice, 48 distinct titles, 48 distinct rationales, 44 distinct next actions, zero splice violations, zero caption-prefix titles, and 10 items with independent verification. Across the four batches: 192 unique ids, no cross-batch duplicate, every id a confirmed member, all fifteen judgement keys present on every item, and 27 items carrying verification from a real credibility file.

- 2026-09-20 — `catalog-records-v3.json` built and independently verified: 665 records (192 confirmed / 405 rejected / 68 LinkedIn, unchanged from v2), confirmed id set equals `ai-collection-membership.json` exactly, all 192 carry a real caption, all fifteen judgement keys present with 0 mismatches against `judgement\batch-1..4.json`, `media_captured: false` on all 192, zero `image_kind: instagram_original`. The 473 non-confirmed records carry only the three classifier fields they already carried in v2 and gained no caption or judgement. `primary_topic` histogram: agents-and-coding 38, design-tools 37, business 23, ai-news 19, cad-and-3d 18, hardware 15, unsorted 13, research 13, security 10, workflows-and-productivity 6 — all nine fixed topics are non-empty, so the corpus owes ten syntheses including `unsorted`. Publication-builder rebuild dispatched against v3.
- 2026-09-20 — pyrgos-ai/doug-harness PR 1536 ("Resolve forced surface model and default quota path") is MERGED; the router defect S1 filed against the harness is now landed, not merely filed.
- 2026-09-20 — SC5 baseline re-measured immediately before publication and is unchanged from 2026-09-19: `Verify-VaultLinks.ps1 -Scope "50 Knowledge" -WarnOnly` reports NOTES=680, LINKS=5762, BROKEN=0, AMBIGUOUS=26, ORPHANS=26, UNREFERENCED_OUTPUTS=0, DUPLICATE_ROOTS=0. The post-publication pass condition stays BROKEN 0 and AMBIGUOUS no greater than 26.
- 2026-09-20 — Publication-builder rebuild against v3 landed and was inspected: 192 source notes, 10 topic syntheses (the nine fixed topics plus `unsorted`), zero `["unresolved"]` list markers outside `Backups`, and contract point 1 now proven — `Images/crucix-dashboard.png` exists and exactly one note embeds it, captioned "Official project image — not Instagram media". `CONTRACT-EVIDENCE.md` claims all twelve points pass; that claim still owes the independent review.
- 2026-09-20 — Defect found by reading, not by a histogram: `primary_topic` in v3 does not survive inspection. "Weekly AI news claims roundup" is assigned `cad-and-3d`, "A week of fast-moving AI headlines" and "Gemini animated logo email tactic" are assigned `hardware`, "Personalized patio outreach for contractors" is assigned `security`. The three smallest buckets appear to have been padded so no topic would be empty, which is the opposite of the rule the merge brief set. The syntheses are the reader-facing product, so they are built on sand until this is fixed. Reassignment packet dispatched: it re-decides every member's topic from title, summary and caption, requires each justification to quote four or more consecutive words from that member's own text, and explicitly permits an empty bucket.
- 2026-09-20 — **Privacy violation found in the publishable corpus and fixed.** `build_social_publication.py` sourced the DM note from the raw `browser/instagram-dm-ai-resources.json` and rendered `t.get('sender')` for every thread, so the dry run's `DM resources.md` published eight DM sender display names. This survived a self-reported twelve-of-twelve contract pass, because no contract point tested it. The builder now sources the bounded, handle-free `browser/dm-resource-index.json` and renders only resource name, kind and URL; the note carries an explicit statement that no sender, display name, handle, thread title, timestamp or message text is retained. Verified after a rerun: 43 public resources published and zero occurrences of any of the eight names anywhere in the tree. Twelve backup copies under `publication-dryrun/Backups/*/DM resources.md` still carried the names and were removed, since retaining them is itself the violation; nothing else was deleted. `build_social_publication.py.bak-presenderfix` preserves the pre-fix builder.
- 2026-09-20 — Contract point 2 (idempotence) independently reproduced rather than trusted: the builder's own before/after manifests cover 210 files and differ in exactly one, `run-manifest.json`, which carries `generated_at` — the stated exclusion. Moved out of the repository root, where the builder had written them outside its owned paths, to `publication-dryrun/_evidence/`.
- 2026-09-20 — Credibility batch 005 landed and is the strongest evidence layer the project has: 25 resources named by `high`-rated members, checked read-only against their own sources. Resolves 24 yes / 1 no; exists_as_described 14 yes / 10 partial / 1 unchecked; gated 13 yes / 12 no. Every one of the 25 records a specific claim-versus-reality gap rather than a verdict, and each names what it did not test — for example the Obsidian post's smart-connections and qmd MCP wiring are not established by the Obsidian home page, and the Cache-to-Cache paper exists but its stated percentage gains and 2.5x latency result were not reproduced. Ten of the twenty-five are `partial`, which is the honest answer for a post that names a real product and then makes an untested claim about it.
- 2026-09-20 — Residual from that packet: it opened one Chrome tab read-only and reported `tabs_closed: false`, saying closure was attempted twice and blocked by another extension UI. The tab belongs to that agent's own session tab group and is not reachable from this session, so it could not be closed here. Future briefs that permit a browser must require the agent to confirm closure before returning, and to report the tab as still open if it cannot.
- 2026-09-20 — Topic reassignment landed as `classification/primary-topics-v2.json` and confirms the padding was real. `cad-and-3d` falls from 18 members to 1, `workflows-and-productivity` rises from 6 to 18, `agents-and-coding` from 38 to 67; 92 of 192 members changed topic. All six quoted defects are corrected: "Weekly AI news claims roundup" and "A week of fast-moving AI headlines" move to `ai-news`, "Gemini animated logo email tactic" and "Personalized patio outreach for contractors" to `business`, "Open-source repositories for AI builders" and "Comparing Sol and Fable on coding economics" to `agents-and-coding`. Anti-gaming checks pass: 192 of 192 justifications quote four or more consecutive words from that member's own caption or title, 192 distinct, zero containing the stable id. The justification frame is nonetheless a template whose distinctness comes entirely from the quoted span — the check is satisfied honestly here, but a future packet should not treat that frame as evidence of independent reasoning.
- 2026-09-20 — Standing decision recorded, reversible: a topic with fewer than three members still gets its synthesis, and that note must open by stating its own thinness with the count. `cad-and-3d` has one member. Dropping it would silently contradict the goal, which names CAD/3D as one of the nine areas; padding it is what produced the defect. Saying plainly that the collection holds almost no CAD/3D material is the only honest answer, and it is itself a true finding about the corpus.
- 2026-09-20 — Correction to the previous entry: the `cad-and-3d` figure of 1 was read from `primary-topics-v2.json` while the reassignment route was still writing it. The final file, timestamped 01:39:10, gives `cad-and-3d` 9 members, not 1, and 86 members changed rather than 92. Final histogram: agents-and-coding 60, design-tools 37, workflows-and-productivity 18, business 12, security 12, research 12, ai-news 11, hardware 11, unsorted 10, cad-and-3d 9. The anti-gaming checks hold on the final file — 192 of 192 justifications quote four or more consecutive words, 192 distinct, 0 splices — and all six quoted defects are corrected. No topic has fewer than three members, so the thin-topic rule recorded earlier has no case to apply to in this run; it stands as a rule.
- 2026-09-20 — Republish from the overlay landed and was verified independently: 192 notes whose `primary_topic` matches the overlay with 0 mismatches and which all carry `primary_topic_rationale`; 10 syntheses, one per non-empty topic; 0 `["unresolved"]` markers outside Backups; 1 embedded image; exactly 25 notes carrying an `## Independent credibility checks` section, matching batch-005's coverage, with no empty sections; all fifteen judgement fields rendered as their own sections on all 192; and the privacy sweep returns 0 for all eight sender names across the whole tree including Backups. `CONTRACT-EVIDENCE.md` now records twelve of twelve with a named deciding command each. Independent review dispatched to an agent that did not write the builder.
- 2026-09-20 — Continuation plan revision 4 written with revision 3 preserved. It carries the privacy lesson well, including the standing rule that an invariant no gate tests is not enforced. Two sections were not updated and were actively misleading: "The next ten work packets" still listed the judgement rerun, the v3 build, the priority-queue rebuild, the official-image contract point and credibility batch 005 as pending, all of which are finished and on disk, and "What is genuinely done, and what is not" still said 144 judgement items, 11 of 12 contract points, and PR 1536 awaiting merge. That is the exact failure the document exists to prevent, since a fresh agent would redo finished work. Correction packet dispatched with each stale fact named and required to be re-derived by command rather than copied, and instructed to list fewer than ten packets if fewer remain rather than padding the list.
- 2026-09-20 — Independent review returned **not ready** with eight blocking findings, and it earned its keep. Verified two of its central claims myself: `DZ7sxpHyfzu`, the existing `geo_grandmasters` note, is NOT one of the 192 members — it is `rejected` in v3 — which means an earlier brief of mine wrongly called it a member; and exactly 21 judgement records carry a JSON null `hype_evidence`, which the builder published as the string `"unresolved"`, fabricating a value. The other six: the builder accepts an arbitrary `--dest` and writes without `--publish`, so the vault-write gate does not structurally exist; all 192 notes mislabel caption provenance, with 141 naming `instagram-saved-posts.json` and 51 `ai-collection-newly-captured.json` while zero name the authoritative `ai-collection-captions-raw.json` the captions actually came from; all ten topic syntheses are link lists with a generic opening and no corpus-level argument and no link to the contract's cross-link owners; the `cad-and-3d` bucket's nine members are mostly general Claude and Fable showcases rather than CAD or 3D; one note describes media that was never captured; a clean rerun produces 209 files against the tree's 210 because the plan copy is a leftover; and `CONTRACT-EVIDENCE.md` claimed 810 wikilinks checked where an independent scan reproduces 204. What the review confirmed clean: 0 fabrication findings in a seeded 20-note sample, 0 private DM occurrences across the whole tree including Backups, 25 credibility sections with 0 field mismatches and 0 empty sections, 192 topic values matching the overlay, and 204 of 204 links resolving with none bare. Fix packet dispatched against all eight.
- 2026-09-20 — Continuation plan correction landed and is accurate: seven remaining packets, explicitly not padded to ten, and a "genuinely done" section that matches the files and still states plainly that no part of the corpus is in the vault and that the twelve-point table is self-reported until a review reproduces it. Two residual errors were corrected directly, because both would have caused a wrong action rather than merely reading wrong: the plan and the staged publish brief both instructed an in-place update of `geo_grandmasters`, whose shortcode `DZ7sxpHyfzu` is `rejected` and not a member. Both now forbid touching it, require its sha256 to be unchanged across the publish run, and require the corpus index to say that note's post is not a collection member. The review's "fail" on contract point 2 is recorded as superseded for the same reason: there is nothing for a member loop to update.
- 2026-09-20 — Fix packet returned and seven of the review's eight findings verify as genuinely fixed. Caption provenance: all 192 notes now name `browser/ai-collection-captions-raw.json` and none names the historical export. Judgement fidelity: 0 notes publish the string "unresolved" for `hype_evidence` and the 21 JSON nulls are preserved as nulls. Media descriptions: a sweep for any sentence saying what a post depicts, shows, pictures or illustrates returns 0. The vault-write gate now exists structurally as `guard_destination`, which refuses a vault-shaped destination without `--publish`. The syntheses now argue: each carries a coherence check with counts, and `cad-and-3d` opens by saying plainly that only 2 of its 9 members match the CAD/geometry test and that the bucket should not be treated as evidence about CAD capability, while `ai-news` reports 7 of 11 and draws a conclusion about reading those saves as prompts to verify rather than as announcements.
- 2026-09-20 — Finding 8 is NOT fixed and I measured it rather than taking the report's word: `CONTRACT-EVIDENCE.md` still asserts `WIKILINKS_CHECKED=810` in both point 3 and point 6. An independent count over the 207 published non-backup Markdown files, excluding `Backups`, `_review` and `_evidence`, gives 256 wikilinks. The first review measured 204 over its own scope, so at least one scope definition is wrong and the published number matches neither. The re-review is dispatched with this stated explicitly, including that it must define the scope its own count covers.
- 2026-09-20 — Resolved the wikilink scope question by measuring every plausible scope of the dry-run tree, so the eventual correction is unambiguous: published tree only (207 files, excluding `Backups`, `_review`, `_evidence`) = 256 wikilinks; including `_review` and `_evidence` (413 files) = 458; everything including all backups (1855 files) = 1924; published tree counting Markdown links as well = 257. **No scope produces 810, and none produces the first review's 204.** 810 is therefore not a scope disagreement but a number that does not correspond to this tree at all, most likely carried forward from an earlier state; 204 was most likely measured before the syntheses gained their contract cross-link owners, which added links. The correct figure for the current published tree is 256 under the stated scope. The evidence file is deliberately left unedited until the re-review finishes, because changing the tree underneath a running review would invalidate it.
- 2026-09-20 — Second independent re-review returned **not ready**, and it closed most of the ground. Of the first review's eight findings, five verify as fixed with its own commands: the vault gate now refuses a vault-shaped destination with `EXIT=1` and leaves the geo note's sha256 unchanged; judgement frontmatter fidelity is `ALL_FIELD_MISMATCHES=0` with the 21 nulls preserved; all 192 notes name the authoritative caption file; all ten syntheses carry a real collective argument and contract cross-links, and noun substitution would not preserve any of them; and a clean rerun now reproduces at `FILES_A=210 FILES_B=210 DIFF_COUNT=0` with the plan present in both. All twelve contract points pass in this review except point 4. The wikilink discrepancy is settled at **254** over a precisely stated scope of 206 non-backup Markdown files excluding `CONTRACT-EVIDENCE.md`, `Backups`, `_review` and `_evidence`; my own 256 had included the evidence report's own links.
- 2026-09-20 — Five fixes still block publication and are dispatched. Seven judgement values still describe media that was never captured, including "The post depicts developer indecision as humor about a coding choice" — these live in the judgement batches, so the batches get a surgical correction with an audit file recording every old and new value. The builder rewords fourteen judgement values between the batch and the visible note body, while frontmatter stays faithful, so the body renderer must emit verbatim. The evidence generator is not location-invariant: it excludes any absolute path containing `_review`, so the mandated clean rerun reports 0 Markdown, 0 frontmatter and 0 wikilinks for a tree holding 207, 206 and 254. Row 7's backup count says 208 where the manifest it names holds 207. And the `cad-and-3d` coherence test omits the literal term `3D`, so it reports 2 of 9 where at least 7 of 9 members are genuine 3D or interactive-prototyping work — the bucket is real and its label is CAD-heavy, which is what the synthesis should say. Contract point 4 will close with PyYAML 6.0.3, which is installed at the Python313 interpreter the reviewer's environment did not find.
- 2026-09-20 — The five-fix packet got four right and exposed a real architectural gap on the fifth. It corrected all seven media-describing values in the judgement batches and recorded each one properly in `media-corrections-20260920.json` with old value, new value and reason — `DcL1SNXIt6A` now reads "The caption frames developer indecision as humor about a coding choice" in `batch-3.json`. **But the published note still carries the old sentence**, because `catalog-records-v3.json` holds a frozen copy of the judgement made at merge time and the builder renders from v3, not from the batches. Correcting the authoritative source therefore does not reach the corpus. Measured: batch-3 corrected, v3 stale, note stale at line 31 and line 86. The fix is the overlay pattern already used for topic — apply the corrections file at render time and leave v3 immutable — and it is generalised so any future correction file is applied rather than silently ignored.
- 2026-09-20 — Second defect, and it is the session's recurring failure in a new costume: the `cad-and-3d` coherence statistic went from 2 of 9, which understated the bucket, to **9 of 9**, which overstates it, with prose that contradicts itself — "9 of 9 members match the topic-specific CAD/3D evidence test, while the remainder are adjacent experiments" leaves no remainder. The independent reviewer read the members and found at least 7 of 9 are genuine 3D or interactive-prototyping work with a CAD-heavy label. Loosening a test until everything passes is padding wearing different clothes. Fix packet dispatched for both defects.
- 2026-09-20 — Method note, recorded because it has now cost two false readings: a waiter that polls a file's modification time fires while a packet is still writing, and the tree it reports is an intermediate state. It first produced a wrong `cad-and-3d` figure of 1 from a half-written topic overlay, and then a wrong "nothing changed" reading of the two-defect fix while the builder was mid-rewrite at 51152 bytes with the corrections overlay not yet wired. The correct signal is the fleet route's own `state` leaving `working` in `%LOCALAPPDATA%\AgentHarness\repositories\douglaspmcgowan\general-ai\fleet\agent-status\route-*.json`. All waiting is now on route state.
- 2026-09-20 — Both defects from the two-defect packet verify fixed. Zero media-describing sentences remain across the corpus, and `DcL1SNXIt6A` now publishes `post_claims: ["The caption frames developer indecision as humor about a coding choice."]`. The corrections mechanism was built generically rather than against one filename: `judgement_correction_paths()` globs the judgement directory, `load_judgement_corrections()` validates that each entry carries `stable_id`, `field`, `old` and `new`, and `apply_judgement_corrections()` raises on a duplicate correction — so a future correction file is applied rather than silently ignored. My own first check for this looked for the literal string `media-corrections` in the builder and returned 0, which would have read as unwired; the generic glob is the better implementation and the grep was the wrong test. `cad-and-3d` now reads "7 of 9 members match the explicit CAD/3D evidence test" in one consistent clause, landing on the number the independent reviewer reached by reading the members, between the 2-of-9 understatement and the 9-of-9 overcorrection.
- 2026-09-20 — Review round three was refused on first dispatch: `Invoke-FleetRoute` rejected the brief for a missing `## Verification` section, which the router requires alongside `## Owned paths`. Added one naming the eight artifacts that decide the lane is done, and re-dispatched successfully. Worth remembering when writing a reviewer brief, whose natural shape ends in a verdict rather than a verification list.
- 2026-09-20 — Contract point 4 closed independently by me rather than left unproven: PyYAML 6.0.3 at the Python313 interpreter parses all 206 non-backup frontmatter blocks in the dry-run tree with 0 failures, each one loading as a mapping. Both reviewers were right to refuse to infer this — round two's environment could not find a parser and correctly left the point unproven rather than accepting the builder's custom structural parser as equivalent.
- 2026-09-20 — Second method defect in the waiters, recorded so it stops recurring: polling "the newest route file by modification time" does not identify the route this session launched. It reported `ROUTE STATE: finished` for `route-38c7298e.json` while review round three was still working under a different route id, with two routes working and one failed in the same window. Route files in this directory carry no role or model field to match on either. The authoritative signal for a dispatch is its own background task completing, which carries the route's own status-file path in its output; waiters should key on the owned artifact appearing, not on a route directory listing.
- 2026-09-20 — Review round three returned **not ready** on exactly one point of twelve, and the fault was in my brief rather than in the corpus. Points 1-11 all reproduce as pass on a clean rebuild the reviewer ran itself: 210 artifacts, 192 source notes matching the membership file with 0 missing and 0 extra, all fifteen judgement fields byte-identical to the batches in both frontmatter and body across 2,880 renderings, 0 fabrication findings, 254 wikilinks with 0 unresolved, PyYAML 6.0.3 parsing 206 of 206, the vault-like-destination guard refusing and creating nothing, and a privacy sweep over 9,174 text files returning 0 sender, handle, thread-URL or timestamp occurrences. All five of round two's required fixes reproduce as fixed, including the media-correction overlay (7 applied, 7 exact, 0 unlisted drift) and the cad-and-3d coherence statistic, which the reviewer independently re-derived as 7 of 9.
- 2026-09-20 — Ruling on the one failure, recorded because it changes a stated gate. My round-three brief contained two lines that cannot both hold: line 38 required that no rejected record appear in "any note, queue, count or link", while line 39 required that the pre-existing vault note `geo_grandmasters — ... — DZ7sxpHyfzu.md` be preserved untouched. That note is itself the note of a rejected record, so any index entry naming it violates line 38 by construction. The reviewer was right to fail the point rather than reinterpret my wording, and said so explicitly. The corrected wording, which is what the goal condition actually requires: a rejected, not-applicable or LinkedIn record must not produce a generated source note, must not enter the needs-review queue, must not be counted as a member, and must not be the target of a wikilink to a note this builder generated. Naming a rejected shortcode in prose as excluded, and linking a pre-existing vault note the builder did not create, are not leakage — SC1 requires every record to carry its membership verdict with an evidence path, so suppressing a visible rejection would make the corpus less honest, not more. Under the corrected wording the reviewer's own measurement decides the point: `membership=192 notes=192 missing=0 extra=0 rejected_note_ids=0 notapp_note_ids=0 linkedin_note_ids=0`.
- 2026-09-20 — The reviewer also reported a conflict it could not resolve: the fleet wrapper asked it to update the branch and open a pull request while the brief forbade touching Git. The brief wins for a reviewer lane, and it correctly did neither. Recorded so the wrapper text and reviewer briefs get reconciled rather than left to each lane.
- 2026-09-20 - Continuation plan revision 5 landed at 41,916 bytes over 311 lines, with revision 4 preserved beside revisions 2 and 3. It records round three verbatim, states the corrected membership gate as the governing definition with the reviewer measurement that passes it, and says plainly that publication is blocked on Douglas authorization rather than on missing evidence, so a resuming agent asks for that instead of re-running reviews. My wrapped-paragraph check flagged 29 suspect points; all 29 are lines inside fenced JSON blocks, which the check failed to exclude, and no prose paragraph is wrapped.
- 2026-09-20 - Fleet router defect fixed and raised as pull request 1586 on pyrgos-ai/doug-harness, branch `agent/fleet-preamble-git-conflict`, not merged. The preamble is now split: the narration and no-questions clause goes on every brief, and the pull-request clause is appended only when the brief does not match `do not touch git` case-insensitively. The idempotence check moved onto the narration clause, which matters because the old check matched the whole line including the PR clause and would have silently stopped working for Git-forbidden briefs. I ran the new test myself rather than trusting the lane report: `FleetPreambleGitScope.test.ps1` gives passed=6 failed=0 across both directions and both idempotence cases. The installed projection at `~/.agents/tools/` still carries the old line and will until the pull request merges and `Manage-Harness -Action InstallGlobal` re-renders it, so briefs dispatched from this machine keep showing the contradiction in the meantime.
- 2026-09-20 - The long-running QA classification packet finally returned, and it had stalled rather than worked: route route-c0273fb9 on opencode with muse-spark-1.3, timed out by the router with reason stalled-no-progress and adapter_exit -1. Its outputs were not lost, because an earlier successful run had already written `qa\classification-audit-20260919.json` and its companion on 2026-09-19; I re-validated the JSON and it parses with all nine expected keys, so nothing needs re-running. Note for whoever reads that pair: the `.md` companion holds the same JSON rather than prose, which is a cosmetic defect in a historical artifact whose findings the membership work has since superseded.
- 2026-09-20 - Second harness defect found while reading that failure, filed as intake `fleet-route-credits-stalled-lane-with-other-lanes-commits`. The same attempt record that says the lane stalled also says `produced_work=True`, `work_detail=commits`, `new_commits=True` and `changed_file_count=0`. The lane wrote nothing; the commits the router counted were mine, made on the same branch while it sat stalled. `produced_work` gates surface fall-through, so a dead lane credited with another session commits can look productive and stop the router from trying the next surface. The record contradicts itself in place - new commits beside zero changed files - which is the cheap check the router is missing. I verified against `git log` that all twelve commits on this branch are accounted for and none came from the lane.
- 2026-09-20 - Pinned the reviewed tree so publication cannot silently publish something else. `qa\reviewed-tree-pin-20260920.json` records the sha256 and size of all 210 files in `publication-dryrun`, excluding `Backups`, `_review` and `_evidence`, plus an aggregate `tree_sha256` of `a7d800f092485a42c5b856df781d7966066c4b5e91fea24052468e573cba1175` over the sorted path-and-hash list. My independent count corroborates round three exactly: 210 files, 207 Markdown, 192 source notes, 10 syntheses. The publication brief now recomputes that aggregate first and stops if it differs, naming every changed, added and missing path. This closes a real gap: the review was of a tree that nothing prevented from drifting before the write, and the write is the one step that cannot be taken back.
- 2026-09-20 - Douglas authorized the vault write ("you can vault write") and asked for categories, an Obsidian canvas of the clusters, analysis and comparison of each category, dedup by the thing rather than the post, and asked whether the DM scan was complete. Answered in chat: no, it covered 8 threads. Tracked as SC12 through SC15.
- 2026-09-21 - The first publication attempt did not publish. The codex lane was refused twice by codex own safety router, which would not accept vault authorization relayed through a brief and classed the retry as a workaround. The builder is byte-identical to its pre-run backup and nothing reached the vault, yet the fleet router returned outcome=succeeded; filed against agent-harness as `fleet-route-succeeded-on-codex-safety-refusal`. Decision, reversible: publish once, after the entity, comparison and canvas layer lands, rather than twice. Two reasons. The builder writes its backups inside the destination, so a second publication into the vault would put backup copies of notes inside Obsidian and create exactly the ambiguous links SC5 forbids; and a separate small publisher that copies a verified, pinned tree into the vault with backups held outside it keeps the builder guard intact instead of removing it, which is the materially safer route codex asked for. That publisher is run by this session under the authorization Douglas gave directly.
- 2026-09-21 - DM scan, second pass, run by this session in Douglas Chrome through the Instagram web inbox API with the filter executing inside the page, so no message text, sender or thread identity ever left the browser: 127 threads in the primary inbox, 0 further threads in the General folder, 282 API pages, 410 distinct shared resources before classification. One long thread hit the 60-page cap and is being read to its end. Message requests were deliberately not read. 338 items came back as `placeholder`, content the web client cannot render, and are counted as unreadable rather than guessed at.
- 2026-09-21 - SC12 closed with `browser\dm-scan-002.json` (8 AI/tech additions; limits recorded; 0 private tokens). Entity layer v1 rejected on quality: 817 entities, 722 singletons, dominated by caption fragments, hashtags and shouted words despite passing all nine mechanical checks - so the brief now carries a code-computed name-quality gate and a 25-singleton sample audit; v2 dispatched on luna (sol refused as named-model-unavailable for builder). The v1 lane also attempted `git push` of this branch because the installed router preamble still carries the PR clause; the push guard refused it and nothing was committed.
- 2026-09-21 - Entity layer v2 rerun completed from the fleet brief in the local staging exception: 68 prior no-entity posts were re-judged (26 now named, 42 still none), family rollups were normalized, and the deterministic aggregator ran twice with identical sha256 `575dbf144e87e85602a4ce5d7be218f4e808353de012f41a618c339caa6687cd`. Evidence checks 1-13 pass: 162 entities, 47 multi-pointer, 115 singleton, pointer sum 400 equals 400 distinct pairs, all 42 surviving reasons are individually worded, pointer-derived unverified flags have 0 mismatches, and the four-file privacy scan is 0. The external fleet status writer remained unproven after access denied; independent review is ready.
- 2026-09-21 - Entity layer v3 accepted: 162 entities, 47 multi-pointer, 42 of 192 posts genuinely name nothing; v2 had under-recalled 68 posts, several of whose own reasons named the product. Dispatched in parallel: the comparisons, entity notes, canvas and DM-note refresh into the staging tree, and a standalone vault publisher with backups outside the vault, tested only against a fake vault.
- 2026-09-21 - Vault publisher `.local/publish_social_corpus.py` built and tested against a fake vault only: dry run by default, backups outside the vault, refuses human-authored, locked and excluded paths, never deletes, never touches the geo note, verifies the reviewed-tree pin. Its unit suite failed 1 of 11 in this shell because the script printed em-dash filenames through a cp1252 console; fixed by forcing UTF-8 stdout and stderr, and the suite now passes 11 of 11 on two consecutive runs. The real vault run waits for the comparisons packet and its review.
